---
description: Add a global Nexus TODO with automatic classification.
---

Add this item to the global Nexus TODO file:

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/nexus.js" add "$ARGUMENTS"
```

Then respond:

```text
Outcome:
Actions:
Evidence:
Next Step:
```

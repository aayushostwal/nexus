# Add TODO

Classify the user's requested task into one label and add it to global Nexus TODOs.

Labels: `Work`, `Python`, `Claude`, `Codex`, `AWS`, `Jira`, `Slack`, `Outlook`, `Personal`, `General`.

Command:

```bash
nexus add "$ARGUMENTS"
```

Respond with:

```text
Outcome:
Actions:
Evidence:
Next Step:
```
